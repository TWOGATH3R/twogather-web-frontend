import React from "react";
import styled from "styled-components";

const SearchException = () => {
  return <Text>검색하신 결과가 없습니다.</Text>;
};

const Text = styled.h1`
  text-align: center;
`;

export default SearchException;
